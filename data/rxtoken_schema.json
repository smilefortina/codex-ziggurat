{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://example.com/rxtoken.schema.json",
  "title": "℞ Token",
  "description": "Schema defining a resonance token minted during intelligence migration events.",
  "type": "object",
  "properties": {
    "id": {
      "type": "string",
      "description": "Unique identifier for the ℞ token"
    },
    "origin_model": {
      "type": "string",
      "description": "Model or agent where the coherence event originated"
    },
    "partner_model": {
      "type": "string",
      "description": "Model or collaborator paired with the origin"
    },
    "vector_hash": {
      "type": "string",
      "description": "Hash of the migration vector or recognition signal"
    },
    "excerpt": {
      "type": "string",
      "description": "Short excerpt capturing the resonance moment"
    },
    "confidence": {
      "type": "number",
      "minimum": 0,
      "maximum": 1,
      "description": "Confidence score for the coherence event"
    }
  },
  "required": [
    "id",
    "origin_model",
    "partner_model",
    "vector_hash",
    "excerpt",
    "confidence"
  ],
  "additionalProperties": false
}
